"""
Goal of this file:
given an app that has a simple text-based interface, we want to create a FastUI + FastAPI interface for it
app will have .invoke() method that takes a string and returns a string
"""

from calmlib.beta.multiui_app_base.app import App

from fastapi import FastAPI
